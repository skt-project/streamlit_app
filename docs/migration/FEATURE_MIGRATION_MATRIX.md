# Feature Migration Matrix

Companion to [MIGRATION_PLAN.md](MIGRATION_PLAN.md). Every live/relevant feature in the `D:\GitHub\streamlit_app` portfolio, audited 2026-08-26. Complexity ratings: Easy / Medium / Hard / Needs redesign, as defined in the task brief. "Status" reflects production state as found, not intent.

## PO Suggestion / PO Portal family

| Feature | Current Implementation | Business Logic | Data Source | File Storage | Complexity | Target Architecture | Status |
|---|---|---|---|---|---|---|---|
| PO Suggestion viewer (per store) | `po_buffer.py` — no login, filter selectboxes, per-store metrics + individual PDF download | Inventory-buffer-derived reorder suggestions | `rsa.inventory_buffer` (BigQuery) | None (in-memory PDF via ReportLab) | Medium | `po-api` — bounded query, PDF generation moved behind explicit per-request action, not render-loop | LIVE PROD |
| Auto-refresh stored procedure trigger | `po_buffer.py` — unauthenticated, fires `CALL rsa.inventory_buffer_sp()` if data >2h stale, no lock | Keep buffer data fresh | `rsa.inventory_buffer_sp` | — | Medium | Move to a scheduled Cloud Scheduler/Airflow job; remove the client-triggerable path entirely | LIVE PROD, needs auth/lock fix regardless of hosting |
| Bulk PDF download (multi-store) | `po_buffer.py` | Same as above, batched | Same | In-memory | Medium | Same service, async job if store count grows large | LIVE PROD |
| PO Suggestion viewer (distributor self-service, legacy schema) | `po_portal_suggestion.py` — plaintext-password login, fetch-all then pandas-filter by company | Distributor-scoped PO quantities | `po_portal_suggestion` (BigQuery, unfiltered fetch) | None | Hard | `po-api` `GET /po/suggestions?company=` — server-side WHERE, real auth | LIVE PROD |
| PO Suggestion viewer (dynamic-matrix schema) | `po_portal_suggestion.py`, promoted 2026-08-20 — runs alongside the legacy path on every load (2x query cost) | Same, spreadsheet-driven dynamic columns | `po_portal_suggestion_matrix` + `_matrix_schema` | None | Hard | Same endpoint, single schema decision made server-side (retire the legacy dual-path during the rewrite) | LIVE PROD |
| PO Order Tracking | `po_portal_suggestion.py` — Admin path unbounded, non-Admin path leading-wildcard `LIKE` | Historical order lookup | `dms.gt_po_tracking_all_mv` | None | Hard | `GET /po/tracking` — default 90-day window (already prototyped in `po_portal_suggestion_v2.py`), exact-match key if one exists instead of `LIKE '%...%'` | LIVE PROD |
| Feedback upload (legacy) | `po_portal_suggestion.py` — `.xlsx` upload, shape-detected, validated, `insert_rows_json` | Distributor feedback capture | `po_portal_feedback` (122k+ rows) | None (in-memory) | Medium | Signed-URL upload → `POST /po/feedback/uploads/*` 3-step flow | LIVE PROD |
| Feedback upload (dynamic) | `po_portal_suggestion.py` — same shape, schema-derived validation, includes a live in-request DDL (`update_table`) | Same | `po_portal_feedback_matrix` | None | Hard | Same endpoint; move the schema-sync DDL to a deploy-time migration step, never in the request path | LIVE PROD |
| Simulator — Request PO (SPV) | `po_simulator_v2.py` default page — drill-down suggestions, Google-Sheet-templated Excel + PDF export | SKU allocation suggestions, lifecycle merge | `rsa.stock_analysis`, `gt_schema.master_product`, an external Google Sheet | None | Medium | `po-api` — read-only, portable once auth is added | LIVE PROD (no effective auth — dead `check_password()`) |
| Simulator — PO Simulator (SPV) | `po_simulator_v2.py` — near-duplicate of `po_simulator.py`'s logic, embedded inline | WOI + Proceed/Reject rule engine | `rsa.stock_analysis`, `master_product`, `npd_allocation` (hardcoded date) | Multi-file upload, in-memory only | Hard | Consolidate with the RSA-page copy into one shared simulation function before porting | LIVE PROD |
| Simulator — PO Simulator (RSA) + QTY modify | `po_simulator_v2.py` — separate password gate, multi-file config UI, Windows-Excel-COM dead code path, QTY edit/delete | Same rule engine + in-place file editing | Same tables | Multi-file upload/edit, in-memory + optional COM temp file (Windows-only, non-functional on Linux hosting) | Needs redesign | Multi-step wizard → staged draft/confirm/commit API pattern; drop the Excel-COM path entirely for a Linux target | LIVE PROD |
| Simulator (v1, standalone) | `po_simulator.py` — no auth at all, single-page | Same rule engine (earlier version) | Same tables | In-memory | Easy-Medium | Confirm with the business whether this standalone app is still linked anywhere before deciding to retire vs. fold into `po_simulator_v2.py`'s port | Quiet since 2026-06-10, not deleted |
| Dynamic-matrix refresh/validation harness | `po_portal_suggestion_dev.py` — manual DAG-equivalent trigger buttons, isolated `_dev`-suffixed tables, WRITE_TRUNCATE | Pre-promotion validation | `po_portal_suggestion_dev`, matrix tables (read), Google Sheet (read) | None | N/A — retire | Convert to an Airflow DAG-trigger UI or CLI, or retire once the dynamic table is fully trusted; not a frontend migration candidate | Active test harness, not production |
| PO Portal fixes (uncached client, WHERE pushdown, bounded tracking, gated exports, retry, capped cache) | `po_portal_suggestion_v2.py` | Same as legacy PO Portal Suggestion | `po_portal_suggestion` only (no matrix) | None | Medium | Already demonstrates the target query/caching pattern — use as the reference implementation for `po-api`'s suggestion/tracking endpoints | Superseded/stale (fixes never merged into what's actually live) |

## Distributor Operational Assessment

| Feature | Current Implementation | Business Logic | Data Source | File Storage | Complexity | Target Architecture | Status |
|---|---|---|---|---|---|---|---|
| Login / session | `skt_area_execution_capability_v2.py` — plaintext password compare, `bcrypt` pinned but unused | 4-role identity (ASS, Distributor Manager, Admin RSA, Account Receivable) | `assessment_users` | None | Hard | `assessment-api` auth, real bcrypt/argon2 hashing (rehash on first login post-migration) | LIVE PROD |
| ASS per-distributor scorecard form | Same file — per-metric radio/text cards, live running score | 7 weighted metrics, 34 pts | `master_distributor`, `pbi_gt_dataset.fact_sell_in_all`, `distributor_assessment` | None | Hard | Staged draft/confirm/commit endpoint replicating the confirm-dialog-then-insert guarantee | LIVE PROD |
| Bulk role scorecards (Distributor Manager / Admin RSA / AR) | Same file — template download, upload, preview, confirm, insert | 3 metrics, 4-8 pts each | `distributor_assessment` | Excel template round-trip, in-memory | Hard | Same pattern as ASS, role-scoped | LIVE PROD |
| NPD/SKU Focus allocation bulk upload | Same file — per-program template, nullable fields, no dedup on insert | Allocation targets per distributor/SKU | `distributor_sku_allocation` | Excel, in-memory | Hard | Signed-URL upload flow; **add the dedup guard that's missing today** during the port, not after | LIVE PROD — highest data-pollution risk in the portfolio (no dedup at all) |
| Create User (admin) | Same file | New login provisioning | `assessment_users` | None | Medium | `POST /assessment/users`, real password hashing | LIVE PROD |
| Change Password (self-service) | Same file — real `UPDATE`, clears session on success | Credential rotation | `assessment_users` | None | Medium | `POST /auth/change-password` | LIVE PROD |
| Reminder emails | `assessment_email.py` — SMTP, mostly pure logic | Notify non-submitted ASS users | `assessment_users` (recipient lookup) | None | Easy | Port almost as-is; replace `st.secrets`/`st.warning` touchpoints with env config + logged errors | LIVE PROD |
| Shared scoring/validation logic | `assessment_logic.py` — pure, 42 tests, 98% coverage | Grade bands, SLA compliance, bad-stock exemptions, allocation-row validation | N/A | N/A | Easy | **Port as-is, unchanged**, into `assessment-api` | Reusable now |
| Original single-role assessment | `skt_area_execution_capability.py` | Superseded scoring model | `distributor_assessment` (same table, incompatible schema) | None | N/A — retire | Archive; do not migrate; risk of schema-mismatched writes if ever run | Dead since 2026-04-08 |

## NOO / SKU Mapping & Field Operations

| Feature | Current Implementation | Business Logic | Data Source | File Storage | Complexity | Target Architecture | Status |
|---|---|---|---|---|---|---|---|
| NOO submission | `noo_sku_mapping.py` + `noo_sku/` — template download, upload, validate→enrich→normalize→identity→dedup→preview→confirm→append | New Outlet Opening capture | Google Sheet ("NOO TRACKER GT" pools), `gt_schema.master_product` (enrichment) | Excel, in-memory | Medium-Hard | `master-data-api`; the pure `noo_sku/` modules port directly, the open question is whether Sheets stays system-of-record or moves to BigQuery (product decision, not an engineering one) | Dry-run verified, zero real writes yet (`write_enabled=false`) |
| SKU Mapping submission | Same package | Customer/SKU mapping capture | Same Sheet, `dms.gt_po_tracking_all_mv` (suffix fallback) | Excel, in-memory | Medium-Hard | Same | Same |
| Login / password change | `noo_sku/auth.py` — bcrypt-hashed password in dedicated BQ table (the one correct auth implementation found in the portfolio) | Distributor self-service identity | `noo_sku_distributor_user` (BigQuery, live writes today for password/login-timestamp) | None | Easy | **Use this as the auth reference implementation for the whole portfolio's consolidation**, not just this app | Live (auth table writes are real today, Sheet writes are not) |
| Duplicate store detector | `noo_detector.py` — no auth, fuzzy match (rapidfuzz + haversine) | Pre-creation duplicate check | `master_store_database_basis` (unfiltered `SELECT *`), `fact_sell_through` (f-string brand filter) | Excel upload or manual form, in-memory | Easy-Medium | `master-data-api`; parameterize the brand filter and add auth before wider exposure (returns NIK/NPWP) | LIVE, no auth (PII exposure) |
| Visit GPS validator | `visit_validator/app.py` | Retrospective QA of salesman-vs-store GPS at check-in | None — pure local file-to-file ETL | Upload/download only, in-memory | Easy | **Reference template** — deploy essentially unchanged, zero external service wiring needed | LIVE, independently deployed already (own `requirements.txt`/`.streamlit` config) |
| Live GPS check-in/out | `sfa_attendance.py` — no identity binding (pick any name), session-state-only pending check-in | Attendance capture with 100m/80m radius gate | `master_store_database_basis` (read), attendance table (insert) | None | Needs redesign | `field-ops-api` — move check-in state to a server-side draft row at check-in time, add real identity binding, add an idempotency key at checkout | LIVE, real unconditional writes, no dedup |

## Sales / Stock Analysis

| Feature | Current Implementation | Business Logic | Data Source | File Storage | Complexity | Target Architecture | Status |
|---|---|---|---|---|---|---|---|
| Salesman roster CRUD | `salesman_pjp.py` — inline edit/replace/deactivate forms | Salesman master maintenance | BigQuery salesman/mapping tables | None | Needs redesign | `master-data-api` CRUD endpoints; the ~140 hardcoded plaintext distributor passwords must be replaced with the portfolio's shared auth, not ported | LIVE PROD |
| PJP template download/upload/replace | Same file — deadline-gated (`INPUT_DEADLINE`, client-side only), delete-then-insert (non-atomic) | Monthly journey-plan snapshot | `gt_master_salesman_pjp` | Excel, in-memory | Needs redesign | Signed-URL upload; **replace to a `MERGE` or transactional multi-statement write**; move the deadline check server-side onto the write endpoint | LIVE PROD — real data-loss risk today if insert fails after delete |
| PJP view/export | Same file | Read-only lookback | Same table | CSV export | Medium | `master-data-api` read endpoint | LIVE PROD |
| Smart Coverage submission | `smart_coverage.py` — no auth, cascading Sheet-driven selects, invoice upload | Per-SKU quantity vs. cluster minimum | Google Sheet (reference data), `gt_schema.smart_coverage` (insert), GCS (invoice files) | Excel/image upload, direct-to-GCS already | Medium | `field-ops-api`; decide whether reference data moves from Sheets to BigQuery | LIVE PROD |
| Store channelization export/upload | `store_channelization.py` — export→edit→re-upload round trip, multi-check validation | Channel assignment per store | Staging BigQuery table | Excel, in-memory | Medium-Hard | `master-data-api`; fix the two non-parameterized queries (one fed by uploaded-file data) during the port | LIVE PROD |
| Stock opname entry | `stock_opname_ssjabo.py` — no auth, region-restricted | Stock count capture, 3 brands | BigQuery product/store reference (read), insert | Optional doc upload, made public via `blob.make_public()` | Easy | `field-ops-api`; remove `make_public()`, use signed URLs | LIVE PROD |
| Top-20 SKU stock entry | `skt_top_20_store_list_stock.py` — same pattern + uncached PO-suggestion reference query | Stock count for a fixed SKU list | Same pattern, plus `skt_top20_po_suggestion` (uncached, unbounded) | Same, public GCS | Easy-Medium | Same service; cache/bound the reference query during the port — this is a real cost bug independent of hosting | LIVE PROD |

## Utilities

| Feature | Current Implementation | Business Logic | Data Source | File Storage | Complexity | Target Architecture | Status |
|---|---|---|---|---|---|---|---|
| Field time-motion stopwatch | `time_study_stopwatch.py` — 7-activity per-visit timers, GPS capture | Time-and-motion study capture | `master_store_database_basis` (read), `gt_salesman_time_motion` (insert — the best-built write path in the portfolio) | None | Needs redesign | `field-ops-api`; **move the 1-second timer/GPS-poll loop to client-side JS**, keep the BigQuery write layer as-is (it's already correct) | LIVE PROD |
| Distributor file → standard schema converter | `template_converter.py` — hardcoded + generic fuzzy-mapping pipelines | Column mapping/dedup to a 12-column master schema | `gt_schema.master_distributor` (read), `distributor_configs` (read + DDL bootstrap) | Excel/CSV, fully in-memory, no temp files | Easy-Medium | `master-data-api`; move the DDL bootstrap to a one-time migration, not the request path | LIVE PROD — near-ideal Cloud Run candidate as-is |
| Whitespace map (original) | `whitespace_map.py` — eager whole-Indonesia load | Nielsen tier + store-grade geospatial overlay | Unfiltered national BigQuery join + GCS geometry/Excel | Excel export only | Hard | `analytics-api`; apply the same offline pre-partitioning technique already proven in its own `_lightweight` sibling before porting | LIVE PROD, highest per-instance memory footprint in the portfolio |
| Whitespace map (lightweight) | `whitespace_map_lightweight.py` — GCS-only, pre-partitioned reads | Same overlay, pre-aggregated | GCS parquet only, no live BigQuery calls | Excel export only | Medium | `analytics-api`; confirm and document the (not-in-repo) offline job that produces the partitioned parquet before treating this as fully portable | LIVE PROD — reference pattern for fixing the original |

## Not migration candidates

| Item | Reason |
|---|---|
| `skt_area_execution_capability.py` | Dead since 2026-04-08; writes to the same live table as v2 with an incompatible schema — archive, do not run or port |
| `skt_area_execution_capability_mock.py` | Test/demo harness with fabricated in-memory data, not a production surface |
| `po_portal/` (utils) | Source files (`auth.py`, `styles.py`) already deleted; only `__pycache__` remnants remain |
| `po_portal_mockup.zip` / `po_portal_mockup/` | Only test/coverage artifacts remain; actual source was never committed or already removed |
| `docs_crawler.py`, `build_html_guide.py`, `build_pptx.py` | No `import streamlit` — internal documentation tooling, not part of the deployed application surface |
